package scanner;

/**
 * The enum of the token types for the expression grammar.
 * @author Erik Steinmetz
 */
public enum ExpTokenType {
    NUMBER,
    PLUS,
    MINUS,
    MULTIPLY,
    DIVIDE,
    LEFT_PAREN,
    RIGHT_PAREN,
}
